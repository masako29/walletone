name: Keep Server Awake

on:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  send-request:
    runs-on: ubuntu-latest

    steps:
    - name: Send GET request
      run: curl -sS https://walletone.onrender.com
